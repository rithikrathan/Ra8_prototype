INSTRUCTION SET FOR THE TEST CPU

assember directives??? idk something will be removed when assembling the the way i type them?? idk english bro dont judge

<quick_maph> : includes or prepend macros that will be prepended to the code before assembling

!name(args)
...
/! : macro definition

!name args : macro call

defines a label that can be used as a jump target or for data storage all code till the next label is considered part of the current label  

$labelname:
	...
	...
	...
$Another_labelname:


$labelname : label calls replaced by the address of the label in the assembled code

$ -> subroutines
! -> macro

Arithmetic instructions:
	ADD -> addition
	ADI -> add immediate
	ADDC -> add with carry
	SUB -> subtraction
	SUI -> subtract immediate
	SUBB -> subtract with borrow
	$MUL -> multiply low byte
	$MUH -> multiply high byte
	$MIL -> multiply immediate low byte
	$MIH -> multiply immediate high byte
	$DIV -> division
	$DII -> divide by immediate
	$REM -> modulo
	$REI -> modulo by immediate
	!INC -> increment  general purpose register ADI Reg 1
	!DEC -> decrement  general purpose register SUI Reg 1
	!NEG -> arithmetic negate 2's complement XRI Reg 0xff; !INC

Logical instructions:
	AND -> and
	ANI -> and immediate
	OR -> or
	ORI -> or immediate
	NOT -> not
	XOR -> xor
	XRI -> xor immediate
	XNR -> xnor
	XNI -> xnor immediate
	IIN -> increment index
	DIN -> decrement index
	CMP -> compare

Bitwise instructions:
	RS -> right shift
	LS -> left shift
	RR -> right rotate
	LR -> left rotate
	ARS -> arithmetic right rotate/shift

Data transfer instructions:
	MV -> move register values
	LD -> load from memory
	LDI -> load immediate
	ST -> store to memory
	STI -> store immediate
	LIN -> load index register 16 bit SI/DI
	SIN -> store index register 16 bit SI/DI
	RIN -> load [Rh|Rl] pair to index reg

	RPC -> register pair to PC
	RSP -> register pair to SP

Condition check instructions:
	CON -> check single flag including 0 for no condition
	COR -> check if any (input - flag mask)
	CAN -> check if all (input - flag mask)


Branching instructions: (Always preceded by a condition check instructions)
	JMP -> jump
	!CALL -> subroutine call
	!RET -> return from subroutine

Machine control:
	NOPE -> no operation
	HLT -> halt
	SET -> set flag

give me a c style array of all instructions with the name opcodes:
const char* instructions[] = {
	"ADD", "ADI", "ADDC", "SUB", "SUI", "SUBB", "$MUL", "$MUH", "$MIL", "$MIH",
	"$DIV", "$DII", "$REM", "$REI", "!INC", "!DEC", "!NEG",
	"AND", "ANI", "OR", "ORI", "NOT", "XOR", "XRI", "XNR", "XNI",
	"IIN", "DIN", "CMP",
	"RS", "LS", "RR", "LR", "ARS",
	"MV", "LD", "LDI", "ST", "STI", "LIN", "SIN", "RIN",
	"RPC", "RSP",
	"CON", "COR", "CAN",
	"JMP", "!CALL", "!RET",
	"NOPE", "HLT", "SET"
};


